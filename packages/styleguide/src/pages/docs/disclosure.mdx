---
title: 'Disclosure'
component: 'Disclosure'
description: "Useful for creating regions of content that can expand/collapse. It helps to hide content that's not immediately relevant to the user."
---

```js live
<Disclosure as={<Button>im disclosure</Button>}>im hide/show</Disclosure>
```

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    },
    as: {
      type: 'ReactNode',
      description: 'The action content to render'
    },
    isVisible: {
      type: 'Boolean',
      description: 'Whether it is visible or not'
    },
    hasMarginBottom: {
      type: 'Boolean',
      description: 'Whether it should have margin bottom or not'
    }
  }}
/>

## Examples

todo
