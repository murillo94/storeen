---
title: 'Side Sheet'
component: 'SideSheet'
description: 'Useful for overlaying the screen on one side. It is used to show more details about a certain content.'
---

```js live removeFragment
() => {
  const [isVisible, setVisible] = React.useState(false);

  return (
    <>
      <Button onClick={() => setVisible(true)}>open side sheet</Button>
      <SideSheet isVisible={isVisible} onClose={() => setVisible(false)}>
        im side sheet
      </SideSheet>
    </>
  );
};
```

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    },
    width: {
      type: 'String',
      description: 'The width of the side sheet'
    },
    isVisible: {
      type: 'Boolean',
      description: 'Whether it is visible or not'
    },
    onClose: {
      type: 'Function',
      description: 'A function to be called when on close'
    }
  }}
/>

## Examples

todo
