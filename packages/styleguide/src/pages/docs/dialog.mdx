---
title: 'Dialog'
component: 'Dialog'
description: 'useful for helping users make decisions and to display critical information.'
---

```js removeFragment
() => {
  const [isVisible, setVisible] = React.useState(false);

  return (
    <>
      <Button onClick={() => setVisible(true)}>open dialog</Button>
      <Dialog isVisible={isVisible} onClose={() => setVisible(false)}>
        im dialog
      </Dialog>
    </>
  );
};
```

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    },
    role: {
      type: 'dialog | alertdialog',
      description: 'The role to apply'
    },
    width: {
      type: 'String',
      description: 'The width of the dialog'
    },
    ariaLabel: {
      type: 'String',
      description: 'The accessibility text (W3C)'
    },
    isVisible: {
      type: 'Boolean',
      description: 'Whether it is visible or not'
    },
    onClose: {
      type: 'Function',
      description: 'A function to be called when on close'
    }
  }}
/>

## Examples

todo
