---
title: 'Menu'
component: 'Menu'
description: 'Useful for displaying a list of contextual actions on left click.'
---

```js live
<Menu text="im menu">
  <MenuGroup>
    <MenuItem>im item</MenuItem>
  </MenuGroup>
</Menu>
```

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    },
    appearance: {
      type: 'default | minimal | negative | positive',
      description: 'The theming to apply'
    },
    id: {
      type: 'String',
      description: 'The id of the menu'
    },
    icon: {
      type: 'String',
      description: 'The icon to apply'
    },
    text: {
      type: 'String',
      description: 'A text of the meun'
    },
    isVisible: {
      type: 'Boolean',
      description: 'Whether it is visible or not'
    },
    hasBorder: {
      type: 'Boolean',
      description: 'Whether it should have border or not'
    },
    onClick: {
      type: 'Function',
      description: 'A function to be called when on click'
    }
  }}
/>

# MenuGroup

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    },
    appearance: {
      type: 'default | minimal | negative | positive',
      description: 'The theming to apply'
    },
    id: {
      type: 'String',
      description: 'The id of the menu group'
    },
    isVisible: {
      type: 'Boolean',
      description: 'Whether it is visible or not'
    }
  }}
/>

# MenuItem

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    },
    appearance: {
      type: 'default | minimal | negative | positive',
      description: 'The theming to apply'
    },
    href: {
      type: 'String',
      description: 'The href of the menu item'
    },
    onClick: {
      type: 'Function',
      description: 'A function to be called when on click'
    }
  }}
/>

## Examples

todo
