---
title: 'Table'
component: 'Table'
description: 'Useful to display tabular data in row format.'
---

todo

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    },
    title: {
      type: 'String',
      description: 'A text title of the container'
    },
    optionsTitle: {
      type: 'ReactNode',
      description: 'The options title content to render'
    },
    optionsHeader: {
      type: 'ReactNode',
      description: 'The options header content to render'
    },
    headers: {
      type: 'Array',
      description: 'The header title to render in table heading'
    },
    placeholderSearchSuffix: {
      type: 'String',
      description: 'The placeholder of the input search'
    },
    ariaLabel: {
      type: 'String',
      description: 'The accessibility text (W3C)'
    },
    hasSearch: {
      type: 'Boolean',
      description: 'Whether it should have border or not'
    }
  }}
/>

# TableBody

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    }
  }}
/>

# TableBodyCell

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    }
  }}
/>

# TableHead

<PropsTable
  data={{
    headers: {
      type: 'Array',
      description: 'The title header to render in table'
    }
  }}
/>

# TableHeadCell

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    }
  }}
/>

# TableOptions

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    },
    hasSearch: {
      type: 'Boolean',
      description: 'Whether it should have input search or not'
    },
    placeholderSearchSuffix: {
      type: 'String',
      description: 'The placeholder of the input search'
    }
  }}
/>

# TableRow

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    },
    isBody: {
      type: 'Boolean',
      description: 'Whether it is within body or not'
    },
    isHover: {
      type: 'Boolean',
      description: 'Whether it is hover or not'
    },
    onClick: {
      type: 'Function',
      description: 'A function to be called when on click'
    }
  }}
/>

# TextTableCell

<PropsTable
  data={{
    children: {
      type: 'ReactNode',
      description: 'The content to render'
    },
    text: {
      type: 'String',
      description: 'A text of the table cell'
    },
    textCustomStyle: {
      type: 'Object',
      description: 'The text custom style to apply'
    }
  }}
/>

## Examples

todo
