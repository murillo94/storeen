---
title: 'Input'
component: 'Input'
description: 'Useful for allowing users to enter, update and select text.'
---

```js live
<Input labelText="im label" id="im-id" name="im-name" placeholder="im input" />
```

<PropsTable
  data={{
    appearance: {
      type: 'default | minimal | negative | positive',
      description: 'The theming to apply'
    },
    type: {
      type: '*',
      description: 'The type attribute specifies the type of input'
    },
    innerRef: {
      type: 'React.RefObject',
      description: 'The ref to apply'
    },
    id: {
      type: 'String',
      description: 'The id of the input'
    },
    name: {
      type: 'String',
      description: 'The name of the input'
    },
    value: {
      type: 'String',
      description: 'The value of the input'
    },
    labelText: {
      type: 'String',
      description: 'The label text of the input'
    },
    placeholder: {
      type: 'String',
      description: 'The placeholder of the input'
    },
    icon: {
      type: 'String',
      description: 'The icon to apply'
    },
    mask: {
      type: 'Array',
      description: 'The mask to apply'
    },
    onChange: {
      type: 'Function',
      description: 'A function to be called when the value changes'
    },
    onClick: {
      type: 'Function',
      description: 'A function to be called when on click'
    }
  }}
/>

## Examples

todo
